name: Deploy kyc-create

on:
  push:
    branches: [main, master]
    paths:
      - 'supabase/functions/kyc-create/**'
      - '.github/workflows/deploy-kyc-create.yml'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy kyc-create
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x

      - name: Install Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link to Supabase Project
        run: |
          PROJECT_REF="${{ secrets.SUPABASE_PROJECT_REF }}"
          if [ -z "$PROJECT_REF" ]; then
            PROJECT_REF="xcvcplwimicylaxghiak"
          fi
          echo "Linking to project: $PROJECT_REF"
          supabase link --project-ref "$PROJECT_REF"
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy kyc-create
        run: |
          echo "🚀 Deploying kyc-create..."
          supabase functions deploy kyc-create --no-verify-jwt
          echo "✅ Successfully deployed kyc-create"
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

